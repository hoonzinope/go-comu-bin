cache:
  listTTLSeconds: 30
  detailTTLSeconds: 30

storage:
  local:
    rootDir: "./data/uploads"
  attachment:
    maxUploadSizeBytes: 10485760

delivery:
  http:
    port: 18577
    auth:
      secret: "commu-bin-secret-key"
