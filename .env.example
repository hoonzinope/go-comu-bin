# Copy this file to `.env` and fill in real values before running Docker Compose.

# Docker image published to Docker Hub.
COMMU_BIN_IMAGE=docker.io/<your-user>/commu-bin:latest

# Host config file bind-mounted into the container.
COMMU_BIN_CONFIG_PATH=/Users/hoonzi/Documents/docker_v/go-commu-bin-data/config.yml

# Required JWT secret. Use a long random value.
DELIVERY_HTTP_AUTH_SECRET=replace-with-real-secret

# Persistence backend. Leave sqlite for the legacy file-backed mode, or switch to mysql for the containerized mode.
# DATABASE_DRIVER=sqlite
# DATABASE_DSN=commu_bin:commu_bin@tcp(mysql:3306)/commu_bin?parseTime=true&charset=utf8mb4&loc=UTC&multiStatements=true

# MySQL container defaults used by docker-compose.yml.
# MYSQL_DATABASE=commu_bin
# MYSQL_USER=commu_bin
# MYSQL_PASSWORD=commu_bin
# MYSQL_ROOT_PASSWORD=commu-bin-root

# Optional overrides for local deployment.
# DELIVERY_HTTP_PORT=18577
# STORAGE_PROVIDER=local
# JOBS_ENABLED=true
# ADMIN_BOOTSTRAP_ENABLED=false
